let express,passport,User;_d8a‍.x([["router",()=>router]]);_d8a‍.w("express",[["default",["express"],function(v){express=v}]]);_d8a‍.w("passport",[["default",["passport"],function(v){passport=v}]]);_d8a‍.w("../models/user",[["User",["User"],function(v){User=v}]]);



const router = express.Router();

router.get("/", (req, res) => {
    res.render("landing");
});

router.get("/register", (req, res) => {
    res.render("register");
});

router.post("/register", (req, res) => {
    const newUser = new User({username: req.body.username});
    User.register(newUser, req.body.password, (err, user) => {
        if(err) {
            req.flash("error", err.message);
            return res.redirect("/register");
        }
        passport.authenticate("local")(req, res, () => {
            req.flash("success", "Welcome to WitcherViews " + user.username);
            return res.redirect("/locations");
        });
    });
});

router.get("/login", (req, res) => {
   res.render("login"); 
});

router.post("/login", passport.authenticate("local", {
    successRedirect: "/locations",
    failureRedirect: "/login"
}));

router.get("/logout", (req, res) => {
    req.logOut();
    req.flash("success", "Logged You Out");
    res.redirect("/locations");
})

