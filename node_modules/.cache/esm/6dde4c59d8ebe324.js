let mongoose;_d8e‍.x([["Location",()=>Location]]);_d8e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const locationSchema = new mongoose.Schema({
    name: String,
    image: String,
    desc: String,
    author: {
        id: {
            type: mongoose.Schema.Types.ObjectId,
            ref: "User"
        },
        username: String
    },
    comments: [
        {
            type: mongoose.Schema.Types.ObjectId,
            ref: "Comment"
        }
    ]
});

const Location = mongoose.model("Location", locationSchema);

