let mongoose;_172‍.x([["Comment",()=>Comment]]);_172‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const commentSchema = new mongoose.Schema({
    text: String,
    author: {
        id: {
            type: mongoose.Schema.Types.ObjectId,
            ref: "User"
        },
        username: String
    }
});

const Comment = mongoose.model("Comment", commentSchema);

