let express,Location,Comment,checkCommentOwnership,isLoggedIn;_81c‍.x([["router",()=>router]]);_81c‍.w("express",[["default",["express"],function(v){express=v}]]);_81c‍.w("../models/location",[["Location",["Location"],function(v){Location=v}]]);_81c‍.w("../models/comments",[["Comment",["Comment"],function(v){Comment=v}]]);_81c‍.w("../middleware/index",[["checkCommentOwnership",["checkCommentOwnership"],function(v){checkCommentOwnership=v}],["isLoggedIn",["isLoggedIn"],function(v){isLoggedIn=v}]]);




const router = express.Router({mergeParams: true});

router.get("/new", isLoggedIn, (req, res) => {
    Location.findById(req.params.id, (err, location) => {
        if(err) {
            _81c‍.g.console.log(err);
        } else {
            res.render("comments/new", {location: location});
        }
    });
});

router.post("/", isLoggedIn, (req, res) => {
    Location.findById(req.params.id, (err, location) => {
        if(err) {
            _81c‍.g.console.log(err);
            res.redirect("/locations");
        } else {
            Comment.create(req.body.comment, (err, comment) => {
                if(err) {
                    req.flash("error", "Something went wrong!");
                    _81c‍.g.console.log(err);
                } else {
                    comment.author.id = req.user._id;
                    comment.author.username = req.user.username;
                    comment.save();
                    location.comments.push(comment);
                    location.save();
                    req.flash("success", "Success!");
                    res.redirect("/locations/" + location._id);
                }
            });
        }
    });
});

router.get("/:comment_id/edit", checkCommentOwnership, (req, res) => {
    Comment.findById(req.params.comment_id, (err, foundComment) => {
        if(err) {
            res.redirect("back");
        } else {
            res.render("/comments/edit", {location_id: req.params.id, comment: foundComment});
        }
    });
});

router.put("/:comment_id", checkCommentOwnership, (req, res) => {
    Comment.findByIdAndUpdate(req.params.comment_id, req.body.comment, (err, updatedComment) => {
        if(err) {
            res.redirect("back");
        } else {
            res.redirect("/locations/" + req.params.id);
        }
    });
});

router.delete("/:comment_id", checkCommentOwnership, (req, res) => {
    Comment.findByIdAndRemove(req.params.comment_id, (err, deletedComment) => {
        if(err) {
            res.redirect("back");
        } else {
            req.flash("success", "Comment Deleted!");
            res.redirect("/locations/" + req.params.id);
        }
    });
});

